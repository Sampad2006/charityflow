# Test-only overrides. Guarantees the UI tests always run in SIMULATION
# MODE even when `.env` contains live testnet contract IDs (deployed via
# deploy.sh). Vite loads `.env.test` in `test` mode and it takes precedence.
VITE_REGISTRY_CONTRACT_ID=
VITE_ESCROW_CONTRACT_ID=
